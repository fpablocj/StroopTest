import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'stroop-test',
  webDir: 'dist/stroop-test',
  bundledWebRuntime: false
};

export default config;
